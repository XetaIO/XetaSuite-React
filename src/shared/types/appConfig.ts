/**
 * App configuration returned by the public API endpoint.
 * These values are safe to expose publicly (no auth required).
 */
export interface AppConfig {
    demo_mode: boolean;
    app_name: string;
}
